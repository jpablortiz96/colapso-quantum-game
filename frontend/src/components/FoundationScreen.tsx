export function FoundationScreen() {
  return (
    <main className="grid min-h-screen place-items-center bg-slate-950 px-6 text-slate-100">
      <section aria-labelledby="product-name" className="max-w-2xl text-center">
        <h1 id="product-name" className="text-5xl font-semibold tracking-[0.2em]">
          COLAPSO
        </h1>
        <p className="mt-6 text-xl text-cyan-200">
          Observa antes de que el universo decida por ti.
        </p>
        <p className="mt-3 text-sm uppercase tracking-[0.25em] text-slate-400">
          Foundation build
        </p>
      </section>
    </main>
  );
}
